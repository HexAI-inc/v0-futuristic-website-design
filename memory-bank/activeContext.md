# Active Context

## Current Goals

- Expanded the analytics system to include session tracking, custom events (external links, downloads), device breakdown (Mobile/Desktop), and traffic source analysis. Updated the database schema with an `events` table and new RPC functions for advanced metrics. The admin dashboard now features a device breakdown pie chart and a traffic sources list.

## Current Blockers

- None yet